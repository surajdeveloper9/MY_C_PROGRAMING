// student mark
#include <stdio.h>
int main() {
    int mark;
    printf("enter your mark\n");
    scanf("%d",&mark);
    if(mark>=90)90
    
    printf("excellent");
    else 
    if(mark>=80)
        printf("good");
        else
        if("mark>=50")
            printf("just pass");
        else 
        if(mark<50)
            printf("fail");
    return 0;
}
